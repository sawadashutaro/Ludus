<% if current_user.entries.find_by(tournament_id: tournament.id) %>
    <%= link_to "エントリー済み", tournament_entries_path(tournament), method: :delete, class:"btn btn-success", remote: true %>
   <% else %>
    <%= link_to "エントリー", tournament_entries_path(tournament), method: :post, class:"btn btn-success", remote: true %>
<% end %>